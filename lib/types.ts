export type Review = {
  img: string;
  name: string;
  rating: number;
  review: string;
};

export type Product = {
  name: string;
  price: number;
  image: string;
  description: string;
  link: string;
};
